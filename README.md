# Design-patterns

# Generate simple Typescript projects
    npm init -y
    npm install --save-dev typescript
    npx tsc --init
    mkdir src
    touch src/index.ts or echo > src/index.ts

# Compile and run the project
    npx tsc
    node dist/index.js